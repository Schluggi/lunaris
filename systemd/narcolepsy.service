# systemd unit for narcolepsy (Eight Sleep Pod MQTT bridge).
#
# Install on the Pod (example paths):
#   sudo install -m755 narcolepsy /usr/local/bin/
#   sudo install -m644 systemd/narcolepsy.service /etc/systemd/system/
#   sudo systemctl daemon-reload
#   sudo systemctl enable --now narcolepsy
#
# Broker host/port and other flags: edit this unit, or prefer a drop-in:
#   sudo systemctl edit narcolepsy
# and set e.g.:
#   [Service]
#   ExecStart=
#   ExecStart=/usr/local/bin/narcolepsy --mqtt-host 192.168.1.10 --mqtt-port 1883
#
# Optional environment (MQTT credentials, RUST_LOG): create /etc/default/narcolepsy
#   MQTT_USERNAME=ha
#   MQTT_PASSWORD=secret
#   RUST_LOG=info

[Unit]
Description=narcolepsy MQTT bridge (Frozen/Sensor USART, Home Assistant)
Documentation=https://github.com/your-repo/narcolepsy
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
EnvironmentFile=-/etc/default/narcolepsy
ExecStart=/usr/local/bin/narcolepsy --mqtt-host localhost --mqtt-port 1883
Restart=on-failure
RestartSec=10
StartLimitIntervalSec=300
StartLimitBurst=5

[Install]
WantedBy=multi-user.target
